# import * from './generated/prisma.graphql'
type Mutation {
  PLACEHOLDER: String
}
type Query {
  sessions(
    where: SessionWhereInput
    orderBy: SessionOrderByInput
    skip: Int
    first: Int
  ): [Session]!
}

type User {
  id: ID!
  name: String!
}
