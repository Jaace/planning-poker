projects:
  app:
    schemaPath: "src/schema.graphql"
    extensions:
      endpoints:
        default: ${env:APP_DEFAULT_ENDPOINT}
  prisma:
    schemaPath: "src/generated/prisma.graphql"
    extensions:
      prisma: prisma.yml
